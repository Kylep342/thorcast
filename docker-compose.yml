version: "3"

services:
  thorcast-server:
    build: ./thorcast-server
    ports:
      - "5000:5000"
    env_file:
      - ./thorcast-server/env.list
  thorcast-discord:
    build: ./thorcast-discord
    env_file:
     - ./thorcast-discord/env.list